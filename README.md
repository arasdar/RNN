# Recurrent neural network (RNN)
This repository is dedicated to implementing recurrent neural networks (RNNs) such as vanilla RNN, LSTM, and GRU, and to discussing Transformer (self-attention) as future work.
